---
import { formatDate } from "../config";
import BaseLayout from "./BaseLayout.astro";
import Blogposts from "../components/Blogposts.astro";
import Header from "../components/Header.astro";
import About from "../components/About.astro";
import type { CollectionEntry } from "astro:content";
type Props = CollectionEntry<"blog">["data"];
const { title, description, pubDate } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <main class="row pt-clear mt-clear">
    <div class="container flow">
      <Header
        headerClass="normal"
        headerTitle={title}
        headerMeta={`Published on <time datetime=${pubDate} class='text-date'>${formatDate(
          pubDate
        )}</time>`}
      />
      <!-- <Header headerMeta={formatDate(pubDate)} headerTitle={title} /> -->
      <div class="readable flow">
        <slot />
      </div>
    </div>
  </main>
  <section class="row">
    <div class="container flow">
      <About />
    </div>
  </section>
  <section class="row">
    <div class="container flow">
      <Blogposts currentTitle={title} />
    </div>
  </section>
</BaseLayout>

---
import { getEntryBySlug } from "astro:content";
const entry = await getEntryBySlug("blog", "hello-world");
const { Content } = await entry.render();
import PostLayout from "../layouts/PostLayout.astro";
---

<PostLayout {...entry.data}>
  <Content />
</PostLayout>
